# X投稿プレビュー

X投稿ドラフトをブラウザでプレビュー表示する。

## 引数

- `week` (デフォルト): 今週分（月〜日）
- `日付` (例: 2026-04-02): 指定日のみ
- `all`: 未投稿すべて
- `施設ID` (例: 4 27): 施設画像のみ確認

## 手順

```bash
node scripts/preview-images.mjs $ARGUMENTS
```

引数が空の場合は `week` を使用する。

ブラウザで自動オープンされる。結果を報告する必要はない（ユーザーがブラウザで直接確認する）。
